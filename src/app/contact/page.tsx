import { MainWrap } from '@/components/layouts';

import { Form } from './Form';

export default async function Contact() {
  return (
    <MainWrap>
      <section>
        <Form />
      </section>
    </MainWrap>
  );
}
